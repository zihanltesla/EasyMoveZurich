[phases.setup]
nixPkgs = ['nodejs_18', 'npm-9_x']

[phases.install]
cmds = [
    'npm ci',
    'cd server && npm ci'
]

[phases.build]
cmds = [
    'npm run build:deploy'
]

[start]
cmd = 'cd server && npm start'
